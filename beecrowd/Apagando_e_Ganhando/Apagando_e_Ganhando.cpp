#include <bits/stdc++.h>

using namespace std;

int main() {
  int N, D;

  while (cin >> N >> D && N && D) {
    string s, ans;
    int numero = 0; 

    cin >> s;

    for(int i = 0; i < N; i++){ 
      cout << s[i] << " "; 
    }
  }

  return 0;
}
